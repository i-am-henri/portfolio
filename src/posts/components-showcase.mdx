---
title: "MDX Components Showcase"
date: "2025-07-18"
---

# MDX Components Showcase

Welcome to our **MDX components library**! This post demonstrates all the available components in action.

## Headings

# Heading 1
## Heading 2  
### Heading 3
#### Heading 4
##### Heading 5
###### Heading 6

## Text Formatting

This paragraph demonstrates various **text formatting** options. You can use *italics*, **bold text**, and `inline code` within your content.

> This is a blockquote. It's perfect for highlighting important information or quotes from other sources.

## Code Blocks

Here's a TypeScript example with syntax highlighting:

```typescript
interface User {
  id: number;
  name: string;
  email: string;
}

class UserService {
  private users: User[] = [];

  addUser(user: User): void {
    this.users.push(user);
  }

  getUserById(id: number): User | undefined {
    return this.users.find(user => user.id === id);
  }
}
```

JavaScript example:

```javascript
const fetchUserData = async (userId) => {
  try {
    const response = await fetch(`/api/users/${userId}`);
    const userData = await response.json();
    return userData;
  } catch (error) {
    console.error('Failed to fetch user data:', error);
    throw error;
  }
};
```

## Lists

### Unordered Lists

- React components in markdown
- Syntax highlighting with Shiki  
- Fully typed with TypeScript
- Beautiful default styling
- Easy to customize

### Ordered Lists

1. Install dependencies
2. Create component library
3. Set up MDX processing
4. Convert markdown files
5. Update rendering logic

### Nested Lists

- Frontend Technologies
  - React
  - TypeScript
  - Tailwind CSS
- Backend Technologies  
  - Bun runtime
  - Server-side rendering
- Development Tools
  - Biome for linting
  - MDX for content

## Tables

| Component | File | Description |
|-----------|------|-------------|
| `H1-H6` | `heading.tsx` | Styled heading components |
| `P` | `text.tsx` | Paragraph with proper spacing |
| `Code` | `text.tsx` | Inline code styling |
| `Pre` | `code.tsx` | Code blocks with syntax highlighting |
| `Table` | `table.tsx` | Responsive table layout |
| `Img` | `media.tsx` | Optimized image component |

## Links and Media

Check out the [MDX documentation](https://mdxjs.com) for more information.

Visit [React's official site](https://react.dev) to learn more about React.

---

## Conclusion

This MDX setup provides a powerful way to write content with React components while maintaining the simplicity of markdown syntax. All components are properly styled and optimized for both development experience and runtime performance.
